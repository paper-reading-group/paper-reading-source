<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>MambaVision架构</title>
    <url>/2026/05/13/MambaVision%E6%9E%B6%E6%9E%84/</url>
    <content><![CDATA[<p><img data-src="/paper.pdf" alt="原论文 PDF"></p>
<h2 id="一-论文原本的架构"><a href="#一-论文原本的架构" class="headerlink" title="一.论文原本的架构"></a>一.论文原本的架构</h2><p>![屏幕截图 2026-04-09 200206](C:\Users\kangy\Pictures\Screenshots\屏幕截图 2026-04-09 200206.png)</p>
<p>可以看出来，MambaVision是一个混合架构，优先使用CNN快速得到早期特征，提取到局部细节，然后使用改造后的mamba建模，最后使用self attention补全上下文以及长距离理解</p>
<h2 id="二-针对各个模块的分析"><a href="#二-针对各个模块的分析" class="headerlink" title="二.针对各个模块的分析"></a>二.针对各个模块的分析</h2><p>不同参数量的里面的模块数量是不同</p>
<table>
<thead>
<tr>
<th>模型版本</th>
<th>N1(Stage1)</th>
<th>N2(Stage2)</th>
<th>N3(Stage3)</th>
<th>N4(Stage4)</th>
</tr>
</thead>
<tbody><tr>
<td>MambaVision-Tiny (T)</td>
<td>2</td>
<td>2</td>
<td>8</td>
<td>2</td>
</tr>
<tr>
<td>MambaVision-Small (S)</td>
<td>2</td>
<td>2</td>
<td>12</td>
<td>2</td>
</tr>
<tr>
<td>MambaVision-Base (B)</td>
<td>3</td>
<td>4</td>
<td>18</td>
<td>3</td>
</tr>
<tr>
<td>MambaVision-Large (L)</td>
<td>3</td>
<td>4</td>
<td>24</td>
<td>3</td>
</tr>
</tbody></table>
<h2 id="1-stem"><a href="#1-stem" class="headerlink" title="1. stem"></a>1. stem</h2><p>茎秆层主要的目的是将原本的RGB通道图转化为后续处理的张量，同时完成下采样</p>
<p>src中的实现</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def __init__(self, in_chans=3, in_dim=64, dim=96):</span><br><span class="line">        &quot;&quot;&quot;</span><br><span class="line">        Args:</span><br><span class="line">            in_chans: number of input channels.</span><br><span class="line">            dim: feature size dimension.</span><br><span class="line">        &quot;&quot;&quot;</span><br><span class="line">        # in_dim = 1</span><br><span class="line">        super().__init__()</span><br><span class="line">        self.proj = nn.Identity()</span><br><span class="line">        self.conv_down = nn.Sequential(</span><br><span class="line">            nn.Conv2d(in_chans, in_dim, 3, 2, 1, bias=False),</span><br><span class="line">            nn.BatchNorm2d(in_dim, eps=1e-4),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.Conv2d(in_dim, dim, 3, 2, 1, bias=False),</span><br><span class="line">            nn.BatchNorm2d(dim, eps=1e-4),</span><br><span class="line">            nn.ReLU()</span><br><span class="line">            )</span><br></pre></td></tr></table></figure>

<p>采用了采用了两层卷积下采样，将原本的维度从H * W * 3依次改为了H&#x2F;2 * W&#x2F;2 * C再到H&#x2F;4 * W&#x2F;4 *C</p>
<p>其中输出的通道数由模型大小决定</p>
<p>  indim:</p>
<ul>
<li>Tiny 系列：32</li>
<li>其他系列：64</li>
<li>dim 是基础通道数（Stage 0 的通道数）</li>
<li>Tiny: 80</li>
<li>Small: 96</li>
<li>Base: 128</li>
<li>Large: 196</li>
</ul>
<h2 id="2-ConVBlock"><a href="#2-ConVBlock" class="headerlink" title="2. ConVBlock"></a>2. ConVBlock</h2><p>stem将图片转化为张量的形式之后就进行多次卷积以及总计两次降采样，这个地方比较常见，所以不多阐述，代码展示</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">def __init__(self, dim,</span><br><span class="line">             drop_path=0.,</span><br><span class="line">             layer_scale=None,</span><br><span class="line">             kernel_size=3):</span><br><span class="line">    super().__init__()</span><br><span class="line"></span><br><span class="line">    self.conv1 = nn.Conv2d(dim, dim, kernel_size=kernel_size, stride=1, padding=1)</span><br><span class="line">    self.norm1 = nn.BatchNorm2d(dim, eps=1e-5)</span><br><span class="line">    self.act1 = nn.GELU(approximate= &#x27;tanh&#x27;)</span><br><span class="line">    self.conv2 = nn.Conv2d(dim, dim, kernel_size=kernel_size, stride=1, padding=1)</span><br><span class="line">    self.norm2 = nn.BatchNorm2d(dim, eps=1e-5)</span><br><span class="line">    self.layer_scale = layer_scale</span><br><span class="line">    if layer_scale is not None and type(layer_scale) in [int, float]:</span><br><span class="line">        self.gamma = nn.Parameter(layer_scale * torch.ones(dim))</span><br><span class="line">        self.layer_scale = True</span><br><span class="line">    else:</span><br><span class="line">        self.layer_scale = False</span><br><span class="line">    self.drop_path = DropPath(drop_path) if drop_path &gt; 0. else nn.Identity()</span><br><span class="line"></span><br><span class="line">def forward(self, x):</span><br><span class="line">    input = x</span><br><span class="line">    x = self.conv1(x)</span><br><span class="line">    x = self.norm1(x)</span><br><span class="line">    x = self.act1(x)</span><br><span class="line">    x = self.conv2(x)</span><br><span class="line">    x = self.norm2(x)</span><br><span class="line">    if self.layer_scale:</span><br><span class="line">        x = x * self.gamma.view(1, -1, 1, 1)</span><br><span class="line">    x = input + self.drop_path(x)</span><br><span class="line">    return x</span><br></pre></td></tr></table></figure>

<h2 id="3-MambaMixer-MLP"><a href="#3-MambaMixer-MLP" class="headerlink" title="3. MambaMixer+MLP"></a>3. MambaMixer+MLP</h2><p>这个是主要的框架构成部分</p>
<h4 id="（1）对于原本数据的处理"><a href="#（1）对于原本数据的处理" class="headerlink" title="（1）对于原本数据的处理"></a>（1）对于原本数据的处理</h4><p>输入进来的(B,L,D):L对应视觉图的H和W转为长度为L的向量（不为H*W），D为维度数，实际上对应的就是原本的通道数，经过多次卷积这个地方已经变为了512</p>
<p>原本cnn对于图像的处理都是b*D * H *W，经过了一个window_partition函数（将原本的图像划分为一个一个窗口），然后将这个转化为了当前形状,这个在swin transformer中亦有记载</p>
<p>args:window_size:一个窗口的大小</p>
<p>第一个维度B:原本的batchsize* num_windows,  num_windows &#x3D; (H&#x2F;window_size)*(W&#x2F;window_size)</p>
<p>第二个:window_size^2,即一个窗口的大小</p>
<p>这个时候就可以进入mambamix进行处理</p>
<h4 id="（2）Mambamixer"><a href="#（2）Mambamixer" class="headerlink" title="（2）Mambamixer"></a>（2）Mambamixer</h4><h5 id="初始化阶段"><a href="#初始化阶段" class="headerlink" title="初始化阶段"></a>初始化阶段</h5><p>因为mamba这个是分为两条路来进行的，所以在初始化的时候将原本的维度进行线性投影到原本两倍的维度，然后再进行分割开成两个，方便后面进入两个通道</p>
<p>同时为了保证计算的效率，选择了先降秩，再升秩</p>
<p>同时设计了一维的深度卷积来提取特征</p>
<h5 id="前向传播阶段"><a href="#前向传播阶段" class="headerlink" title="前向传播阶段"></a>前向传播阶段</h5><p>1.首先将上面输入的(B,L,D)进行线性投影变换，得到的是(B,L,2D)，为了适配卷积，将维度转化维(B,2D,L),再使用chunk进行分割,分为两个(B,D,L),相当于对于双通道的差分</p>
<p>2.接着对双分支采用深度卷积以及Silu激活函数，这个时候没有原本的Mamba的因果约束（即可以看到后面的，毕竟原本的mamba是用来做语言预测的，看到后面的也就没什么好预测的了）</p>
<p><img data-src="C:\Users\kangy\AppData\Roaming\Typora\typora-user-images\image-20260410153127015.png" alt="image-20260410153127015"></p>
<p>3.现在都激活完之后左侧要做的就是ssm，实现的是mamba的输入依赖选择性机制，而这个“ssm”和原本的ssm是不同的</p>
<p><img data-src="C:\Users\kangy\AppData\Roaming\Typora\typora-user-images\image-20260411192233518.png" alt="image-20260411192233518"></p>
<p>这个是ssm的公式，其中xt为当前输入，ht为隐藏状态，并且ABCD都在训练不断训练，最后固定的参数，对于不同的输入，使用相同的记忆权重处理，而mamba的选择性ssm的BC dt[t]怎是由输入的xt动态形成的，训练的是生成BC的权重，而不直接生成,这个就是选择性ssm强的地方</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">self.x_proj = nn.Linear(</span><br><span class="line">            self.d_inner//2, self.dt_rank + self.d_state * 2, bias=False, **factory_kwargs</span><br><span class="line">        )</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">x_dbl = self.x_proj(rearrange(x, &quot;b d l -&gt; (b l) d&quot;))</span><br><span class="line">        dt, B, C =  torch.split(x_dbl, [self.dt_rank, self.d_state, self.d_state], dim=-1)</span><br></pre></td></tr></table></figure>

<p>上面的是将原本的x线性映射，下面则是映射后对其进行分割为这几个参数其中d_state作为隐藏层维度的大小，就是ht的</p>
<p>维度数，也相当于b,c的大小</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">y = selective_scan_fn(x, </span><br><span class="line">                              dt, </span><br><span class="line">                              A, </span><br><span class="line">                              B, </span><br><span class="line">                              C, </span><br><span class="line">                              self.D.float(), </span><br><span class="line">                              z=None, </span><br><span class="line">                              delta_bias=self.dt_proj.bias.float(), </span><br><span class="line">                              delta_softplus=True, </span><br><span class="line">                              return_last_state=None)</span><br></pre></td></tr></table></figure>

<p>这个是ssm最核心的计算输出的步骤，前面实际上还有一堆rearrange的操作，这里都省去</p>
<p>这个主要做的就是从一个序列先进行离散化，然后一个位置一个位置进行扫描，然后每个位置都会有一个ht，</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for t in range(196):</span><br><span class="line">    # Step 1: 处理 dt</span><br><span class="line">    dt_t = F.softplus(dt[:, :, t] + bias)  # (1, 256)</span><br><span class="line">    # Step 2: 离散化 A</span><br><span class="line">    A_discrete = torch.exp(dt_t.unsqueeze(-1) * A)  # (1, 256, 8)    </span><br><span class="line">    # Step 3: 离散化 B</span><br><span class="line">    B_discrete = dt_t.unsqueeze(-1) * B[:, :, t].unsqueeze(1)  # (1, 256, 8)</span><br><span class="line">    </span><br><span class="line">    # Step 4: 状态更新</span><br><span class="line">    h = A_discrete * h + B_discrete * x[:, :, t].unsqueeze(-1)  # (1, 256, 8)</span><br><span class="line">    # Step 5: 输出计算</span><br><span class="line">    y[:, :, t] = torch.sum(C[:, :, t].unsqueeze(1) * h, dim=-1) + D * x[:, :, t]  # (1, 256)</span><br></pre></td></tr></table></figure>

<p>所以这个里面的dt_t不同，则A,B _discreate不同，产生的h就不同，从而影响最后的输出y</p>
<p>到这个地方实际上就得到了输出y了，但是毕竟是两个拼接出来的，实际上这个还要做一个线性转换out_proj变为传进来的样子在传回去</p>
<p>经过了数次的Mamba之后就进入了MLP，这个使用的是timm库中导入的标准的全连接经过两层全连接以及Gelu激活，维度并没发生变化</p>
<h2 id="4-自注意力"><a href="#4-自注意力" class="headerlink" title="4.自注意力"></a>4.自注意力</h2><p>这个设计的和原本的self attention相同，只不过原本的是针对全局的做自注意力，这个是针对分的窗口来做，所以不在赘述</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>其中stage1,2都为CNN进行快速提取特征，而3,4都采用了相同的基本结构，只是在层数设计上有所不同</p>
<hr><h3>📎 相关论文推荐</h3><ul><li><a href="2026/05/13/papers_titles/">你的论文标题</a></li></ul>]]></content>
      <categories>
        <category>论文精读</category>
      </categories>
      <tags>
        <tag>CV</tag>
        <tag>Mamba</tag>
        <tag>SSM</tag>
        <tag>Transformer</tag>
      </tags>
  </entry>
  <entry>
    <title>你的论文标题</title>
    <url>/2026/05/13/papers_titles/</url>
    <content><![CDATA[<hr><h3>📎 相关论文推荐</h3><ul><li><a href="2026/05/13/MambaVision架构/">MambaVision架构</a></li></ul>]]></content>
      <categories>
        <category>论文精读</category>
      </categories>
      <tags>
        <tag>Transformer</tag>
        <tag>NLP</tag>
      </tags>
  </entry>
</search>
